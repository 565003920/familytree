spring:
  application:
    name: familytree-backend
  datasource:
    url: jdbc:mysql://localhost:3306/family_tree?useUnicode=true&characterEncoding=utf8
    username: root
    password: '@wecredo2016'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379

server:
  port: 8080
